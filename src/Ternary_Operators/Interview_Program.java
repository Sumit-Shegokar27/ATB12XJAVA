package Ternary_Operators;

public class Interview_Program {
    public static void main(String[] args) {

        double age =23.5;

        String result = (age > 18) ? (age > 23 ? " You can drink Alcohol in goa" :" You can't drink Alcohol") :
                " You can't go to goa";
        System.out.println(result);


    }
}
